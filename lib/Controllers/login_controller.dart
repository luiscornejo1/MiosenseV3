class LoginController {
  Future<void> loginUser(String email, String password) async {
    // Aquí iría la lógica para autenticar al usuario
    // Por ejemplo, conectarse a una API o Firebase
    print('Login con: $email y $password');
  }
}